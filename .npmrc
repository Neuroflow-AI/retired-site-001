# Skip the implicit dependency re-check pnpm runs before `pnpm <script>`.
# Builds are driven explicitly in CI, so the check only adds noise/false
# failures over ignored optional build scripts (sharp/esbuild).
verify-deps-before-run=false
